# Contributing

Keep evaluation deterministic and framework-independent. Add behavior tests for rule changes. Never add network calls, secrets, arbitrary command execution, or unbounded input handling.